# G-Navi 프로그램 개요 및 매뉴얼

## 1. 프로그램 개요

### 1.1 프로그램 정보

| 항목 | 내용 |
|------|------|
| 프로그램명 | G-Navi (지-내비) |
| 버전 | 1.0 |
| 개발자 | 강남구 세무관리과 |
| 개발 기간 | 2026년 5월 |
| 실행 환경 | Windows 10/11, Chrome/Edge 브라우저 |
| 기술 스택 | HTML5, JavaScript (ES6+), CSS3 |
| 라이선스 | 자체 개발 (저작권 등록) |

### 1.2 프로그램 목적

G-Navi는 **현장조사 경로 최적화 프로그램**입니다.

지방세 체납 현장조사, 재산조사 등 다수의 현장 방문이 필요한 업무에서, 엑셀로 관리하는 방문지 목록을 불러와 **최적의 방문 순서를 자동 계산**하고, 구간별 네비게이션 링크(QR 코드)를 생성하여 현장 업무의 효율성을 높입니다.

### 1.3 주요 기능

1. **TSP 경로 최적화**: Nearest Neighbor + 2-opt 알고리즘으로 다수의 방문지를 최단 경로로 정렬
2. **엑셀 연동**: 엑셀 파일(.xlsx)에서 주소 목록을 드래그앤드롭으로 불러오기
3. **지오코딩**: Kakao REST API 우선, Nominatim(OpenStreetMap) 폴백으로 주소→좌표 변환
4. **카카오맵 지도 표시**: 출발지/경유지/목적지를 지도에 마커와 경로선으로 표시
5. **네비게이션 연동**: 구간별 네이버 지도/카카오맵 길찾기 URL 자동 생성
6. **QR 코드**: 각 구간의 네비게이션 링크를 QR 코드로 생성 (스마트폰 스캔용)
7. **경로표 인쇄**: 경로표 + QR 코드를 포함한 인쇄용 페이지 생성
8. **경로표 엑셀 내보내기**: 계산된 경로를 엑셀 파일로 저장
9. **이메일 공유**: 전체 경로 및 구간별 네비게이션 링크를 이메일로 발송
10. **드래그 순서 변경**: 계산된 경로를 수동으로 드래그하여 순서 조정 가능
11. **다양한 이동수단**: 자동차, 대중교통, 도보, 혼합(거리 기준 자동 판단) 지원
12. **출발/도착 모드**: 왕복, 엑셀 기준 출발/도착, 수동 입력 등 4가지 모드

### 1.4 기술 아키텍처

```
G-Navi/
├── index.html          ← 메인 페이지 (UI 구조)
├── css/
│   └── style.css       ← 스타일시트
├── js/
│   ├── config.js       ← 설정 (API 키, 기본값)
│   ├── app.js          ← 앱 초기화, 이벤트 바인딩, 상태 관리
│   ├── tsp.js          ← TSP 최적경로 알고리즘 (Haversine + NN + 2-opt)
│   ├── geocoder.js     ← 주소→좌표 변환 (Kakao API + Nominatim 폴백)
│   ├── map.js          ← 카카오맵 지도/마커/경로 렌더링
│   ├── file-loader.js  ← 엑셀 파일 파싱 (SheetJS)
│   ├── share.js        ← 네비게이션 URL 생성, 구간 분할 로직
│   └── route-panel.js  ← 경로 결과 패널, 인쇄, QR, 이메일
├── lib/
│   └── xlsx.full.min.js ← SheetJS 라이브러리 (엑셀 처리)
├── icon.ico            ← 프로그램 아이콘
├── icon.svg            ← SVG 아이콘
├── setup.vbs           ← 바탕화면 바로가기 생성 스크립트
└── G-Navi 실행.bat     ← 실행 배치 파일
```

### 1.5 사용 라이브러리

| 라이브러리 | 라이선스 | 용도 |
|-----------|---------|------|
| Bootstrap 5.3 | MIT | UI 프레임워크 (CDN) |
| Kakao Maps SDK | Kakao 이용약관 | 지도 표시, 주소 검색 (CDN) |
| QRCode.js | MIT | QR 코드 생성 (CDN) |
| SheetJS (xlsx) | Apache-2.0 | 엑셀 파일 읽기/쓰기 (로컬 번들) |

**AGPL 라이브러리 미사용** — 모든 외부 라이브러리가 MIT, Apache-2.0 등 허용적 라이선스입니다.

### 1.6 실행 방식

- **서버 불필요**: 로컬 HTML 파일로 직접 실행 (file:// 프로토콜)
- `G-Navi 실행.bat` 더블클릭 또는 `index.html` 직접 열기
- `setup.vbs` 실행 시 바탕화면에 바로가기 아이콘 자동 생성
- Kakao Maps API 키 미설정 시에도 Nominatim 폴백으로 기본 동작

---

## 2. 사용 매뉴얼

### 2.1 설치 및 실행

1. G-Navi 폴더를 원하는 위치에 복사합니다.
2. `setup.vbs`를 더블클릭하면 바탕화면에 G-Navi 바로가기가 생성됩니다.
3. 바로가기 또는 `G-Navi 실행.bat`을 더블클릭하여 실행합니다.
4. Chrome 또는 Edge 브라우저에서 자동으로 열립니다.

### 2.2 초기 설정 (Kakao Maps API 키)

지도 표시와 정확한 주소 검색을 위해 Kakao Maps API 키를 설정합니다.

1. 우측 상단 톱니바퀴(설정) 버튼을 클릭합니다.
2. [Kakao Developers](https://developers.kakao.com) 에서 앱을 생성합니다.
3. JavaScript 키와 REST API 키를 복사하여 설정 화면에 입력합니다.
4. 플랫폼 > Web > 사이트 도메인에 `file://` 또는 `http://localhost:8282`를 등록합니다.
5. "저장 후 새로고침" 버튼을 클릭합니다.

> API 키 없이도 Nominatim(OpenStreetMap) 기반으로 기본 동작합니다.
> 단, 지도 표시 없이 경로 계산만 가능합니다.

### 2.3 경유지 입력

#### 엑셀 파일 업로드
1. 좌측 패널의 "경유지" 영역에 엑셀 파일(.xlsx)을 드래그앤드롭합니다.
2. 또는 클릭하여 파일 선택 대화상자에서 선택합니다.
3. 엑셀 양식: 1열에 주소, 2열에 메모(선택)
4. "양식" 버튼으로 빈 양식을 다운로드할 수 있습니다.
5. 업로드 즉시 자동 지오코딩(주소→좌표 변환)이 진행됩니다.

#### 수동 주소 추가
1. 주소 입력란에 도로명주소를 입력합니다.
2. "추가" 버튼을 클릭하거나 Enter 키를 누릅니다.
3. 자동으로 지오코딩되어 경유지 목록에 추가됩니다.

### 2.4 출발지/목적지 설정

4가지 모드를 선택할 수 있습니다:

| 모드 | 설명 |
|------|------|
| **출발지=목적지 (왕복)** | 출발지 주소를 입력하면 목적지도 동일하게 설정 (기본값) |
| **엑셀 기준 출발/도착** | 엑셀 첫 번째 주소 → 출발, 마지막 주소 → 도착 |
| **출발지 입력 + 엑셀 마지막 도착** | 출발지는 직접 입력, 도착지는 엑셀 마지막 주소 |
| **출발지/목적지 각각 입력** | 출발지와 목적지를 각각 검색하여 설정 |

출발지/목적지 검색 시 후보 목록이 표시되며, 원하는 주소를 클릭하여 선택합니다.

### 2.5 이동수단 선택

| 이동수단 | 설명 |
|---------|------|
| **자동차** | 자동차 기준 경로 (기본값) |
| **대중교통** | 대중교통 기준, 구간별 1:1 분할 |
| **도보** | 도보 기준 경로 |
| **혼합** | 직선 1.2km 이하는 도보, 초과는 대중교통 자동 판단 |

### 2.6 경로 계산

1. 경유지 입력, 출발지/목적지 설정, 이동수단 선택을 완료합니다.
2. "경로 계산" 버튼을 클릭합니다.
3. TSP 알고리즘이 최적 방문 순서를 계산합니다.
4. 결과가 지도와 좌측 패널에 동시에 표시됩니다.

### 2.7 경로 결과 활용

#### 네비게이션 열기
- "네이버 길찾기" 버튼: 해당 구간의 네이버 지도 길찾기가 새 탭에서 열립니다.
- 경유지 4개 초과 시 자동으로 구간이 분할됩니다.

#### QR 코드
- "QR" 버튼: 해당 구간의 네비게이션 링크가 QR 코드로 표시됩니다.
- 스마트폰으로 QR 코드를 스캔하면 네이버 지도 앱이 열립니다.

#### 링크 복사
- "복사" 버튼: 해당 구간의 네비게이션 URL이 클립보드에 복사됩니다.

#### 경로표 인쇄
- "경로표 인쇄" 버튼: 방문 순서, 주소, 메모, 구간별 QR 코드가 포함된 인쇄용 페이지가 생성됩니다.
- 인쇄하여 현장에 가져갈 수 있습니다.

#### 경로표 엑셀 내보내기
- "경로표 엑셀" 버튼: 계산된 경로를 엑셀 파일로 저장합니다.

#### 이메일 보내기
- "전체 경로 이메일 보내기" 버튼: 전체 경로표와 구간별 네비게이션 링크를 이메일로 발송합니다.

#### 순서 수동 변경
- 경유지 옆의 드래그 핸들(☰)을 잡고 위/아래로 이동하면 방문 순서를 수동으로 변경할 수 있습니다.
- 순서 변경 시 경로가 자동으로 재계산됩니다.

### 2.8 설정 변경

우측 상단 톱니바퀴 버튼으로 설정 모달을 열 수 있습니다:
- Kakao JavaScript API 키
- Kakao REST API 키
- 기본 출발지 주소

설정은 브라우저의 localStorage에 저장되며, "저장 후 새로고침" 시 반영됩니다.

---

## 3. 핵심 알고리즘 설명

### 3.1 TSP (Traveling Salesman Problem) 경로 최적화

다수의 방문지를 최단 경로로 방문하는 문제를 해결합니다.

**1단계: Nearest Neighbor (최근접 이웃)**
- 출발지에서 시작하여 가장 가까운 미방문 경유지를 순서대로 선택
- 초기 경로를 빠르게 생성

**2단계: 2-opt 개선**
- 초기 경로에서 두 간선을 교차하여 뒤집는 것이 더 짧으면 적용
- 교차 경로(crossing)를 제거하여 경로 길이를 줄임
- 출발지와 목적지는 고정, 경유지 구간만 최적화
- 개선이 없을 때까지 반복

**거리 계산: Haversine 공식**
- 지구를 구로 가정하고 두 좌표 간 대원 거리를 계산
- 지구 반지름 6,371km 기준

### 3.2 지오코딩 (주소→좌표 변환)

4단계 폴백 전략으로 높은 성공률을 보장합니다:

1. **Kakao REST API 주소검색** — 가장 정확, 도로명/지번 주소 지원
2. **Kakao REST API 키워드검색** — 건물명, 약식 주소 등
3. **Kakao JS SDK** — Maps SDK 로드 시 사용
4. **Nominatim (OpenStreetMap)** — API 키 불필요, 한국 주소 지원

Nominatim 사용 시 주소 정제 로직:
- 인명 괄호, 법정동, 동호수 자동 제거
- 단계별 간략화: 전체 주소 → 도로명+번호 → 시도 제거 → 면/읍 제거

### 3.3 구간 분할 (네비게이션 URL 생성)

카카오맵 경유지 최대 3개 제한을 고려하여 자동 구간 분할:

- **자동차/도보**: 경유지를 3개씩 묶어 구간 생성
- **대중교통**: 모든 경유지를 1:1 구간으로 분할 (경유지 미지원)
- **혼합**: 직선거리 1.2km 기준으로 도보/대중교통 자동 판단, 연속 도보 구간은 하나로 묶음

---

## 4. 저작권 정보

- 프로그램명: G-Navi
- 저작권자: 강남구
- 개발: 강남구 세무관리과
- 등록 연도: 2026년

본 프로그램은 강남구 세무관리과에서 현장조사 업무 효율화를 위해 자체 개발한 소프트웨어입니다.
